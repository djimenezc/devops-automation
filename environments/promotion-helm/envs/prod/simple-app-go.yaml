apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: simple-app-go
  namespace: argocd
spec:
  destination:
    namespace: prod
  sources:
    - chart: helm-simple-go-app
      targetRevision: 0.1.0
      helm:
        valueFiles:
          - $values/environments/promotion-helm/envs/prod/simple-app-go-values.yaml
          - $values/environments/promotion-helm/envs/prod/simple-app-go-version.yaml
    - repoURL: 'https://github.com/djimenezc/devops-automation'
      ref: values
      targetRevision: feature/environment-promotion
